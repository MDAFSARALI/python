size=eval(input("Enter the no of dollar sign\n"))
for i in range (size):
    for j in range(size):
        if i==0 or i==size : 
            print ("$"*(size))
            print (" "*(size-2))
            if j==0 or j==size:
                print ("$"*(size))
            print (" "*(size-2))
            
   # This problem is not completed its output is not same as the mam has given the question.         
