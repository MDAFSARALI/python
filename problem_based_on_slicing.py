# In the python language we generally us (+)sign to concatianate the two strings ;
# Sclicing is a process in which upto the the (3-1) digit only will print ;
# if/in print we use a[0] then A will be printed only :so on   
b="Good Morning "
a="Afsar"
print (a[0:3])   #----->Slicing.
print (a[:3])    #if we dont initialize then it automatically initialize to 0.
print (a[0:])    #if we dont initialize the last (upto which) then it automatically initialize to last.
print (a[1:4])   #----->Slicing.
print (a[0])     #----->indication of 1st number.
print (b+a)      #----->Concatination of two string.
print (a[-1])    #----->In the condition if we don't know how many digits are there then start with (-1) it will print the last digit forthe second last digit (-2) and so on ;
print (a[-2])    #use of  negative indices.
print (a[-3]) 
print (a[-4]) 
print (a[-5]) 
print(a[-4:-1])

